function logNewPerson() {
    "use strict";

    var person2;
    persom2 = {};
    console.log(persom2);
}

var person;
// persom = {}; // persom is not defined error
console.log(persom);
logNewPerson();


// Either put "use strict" at the very top of the page for everything to use it or you can put it at the top inside of a function for that function to use it
